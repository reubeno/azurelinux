[components.linenoise]
# Release: 13.20200312git%{shortcommit}%{?dist}
release = { calculation = "static", counter = { source = "release-tag", regex = '^([0-9]+)\.[0-9]+git%\{shortcommit\}%\{\?dist\}$' } }
